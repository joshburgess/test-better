//! `cargo-test-better`: optional pretty runner binary.
//!
//! Phase 0 scaffolding: the entry point exists so the workspace builds; the
//! runner itself is implemented in Phase 9 (PROJECT_BUILD_PLAN.md §14).

fn main() {}
